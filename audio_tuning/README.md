# Audio Tuning: диагностика автомобильной аудиосистемы

Локальная программа для повторяемого сравнения DSP-пресетов и точной доводки
автомобильного аудиотракта методом ESS. Пользовательских режима ровно два:

- `quick` - один замер для быстрого отбора вариантов;
- `full` - шесть фиксированных позиций для точной проверки и доводки DSP.

Генерация сигнала, запись, коррекция clock drift, ESS-деконволюция и отчёт теперь
являются внутренними стадиями этих режимов. Названия «режим 2» и «режим 4» больше
не используются. Одноразовые валидации собираются сериями обычных quick-замеров,
поэтому третьего режима не появляется. Техническое описание приведено в
[ALGORITHM.md](ALGORITHM.md), порядок валидации - в [VALIDATION.md](VALIDATION.md).

## Установка

```powershell
cd C:\Projects\adjCarHead\audio_tuning
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

## Профили системы и микрофона

Автомобильная система описывается JSON схемы `car_audio_system_v2` в `devices/`,
а микрофон - отдельным JSON схемы `measurement_microphone_v1` в `microphones/`.
Старые профили `car_audio_device_v1` читаются для совместимости. Текущая машина
описана в `devices/aura_indigo_877dsp_mkii.json`, микрофон - в
`microphones/oklick_sm_700g.json`.

Профиль системы хранит:

- диапазон и длительность ESS для обоих режимов;
- наличие сабвуфера и целевую кривую `neutral_driver` или `warm_driver`;
- уровни источника и магнитолы, Loudness, баланс, фейдер и другие обработки;
- ссылку на профиль микрофона, а тот хранит паспортный диапазон, направленность,
  ориентацию, характеристики и необязательный индивидуальный CSV калибровки;
- реальные DSP-регуляторы: идентификатор, частоту, Q, пределы и шаг;
- результаты одноразовой проверки микрофона, громкости и DSP-регуляторов;
- задержки магнитолы как внешние значения в сантиметрах или миллисекундах.

Aura INDIGO-877DSP MkII зафиксирована как система без сабвуфера. Рабочая опорная
точка: AUX 3,5 мм, ноутбук 65%, магнитола 24/62, двигатель работает. Loudness и
Bass Boost выключены, EQ до начала настройки был плоским, Balance и Fader стоят
по центру. Эти параметры нельзя менять внутри сравнительной серии.

Микрофон Oklick SM-700G закреплён на стойке горизонтально в центре между ушами
водителя. Фронт капсюля направлен к центру лобового стекла/торпедо. Положение
стойки, сиденья и ориентацию микрофона нельзя менять внутри сравнительной серии.

EQ Aura Lab имеет больше центров, чем 31 третьоктавная измерительная сетка:
например, отдельно доступны 3; 3,5; 4; 4,5 и 5 кГц. Gain изменяется с шагом
1 дБ. Для каждой полосы доступен Q от 0 до 12 с шагом 0,5; исходное и текущее
опорное значение Q равно 6. Повторяемость подтверждена сериями 3/3 для исходного
профиля и текущего кандидата. Точная DSP-рекомендация всё ещё требует измеренной
матрицы воздействия и подтверждённой уровневой линейности.

Задержки сохраняются для воспроизводимости, но программа их не измеряет и не
оптимизирует.

## Список устройств

```powershell
.\.venv\Scripts\python.exe audio_tuning.py --list-devices
```

Номера входа и выхода нужно проверять после каждого переподключения USB-микрофона
или аудиоустройства.

## Быстрый режим

Микрофон устанавливается на стойке горизонтально в центре между ушами водителя.
На один preset выполняется ровно одно воспроизведение 5-секундного ESS.

```powershell
.\.venv\Scripts\python.exe audio_tuning.py quick `
  --device-profile .\devices\aura_indigo_877dsp_mkii.json `
  --profile-name preset_candidate `
  --input-device 42 `
  --output-device 21 `
  --channel stereo `
  --split-streams
```

Первый прогон становится baseline. Для последующих вариантов передаётся его
`ess_response.json`:

```powershell
  --baseline-result .\data\outputs\aura_indigo_877dsp_mkii\quick\run_..._center\ess_response.json
```

Режим выдаёт `technically_better_candidate`, `technically_worse`,
`candidate_rejected_frequency_coverage_regression` или
`technically_equivalent_or_inconclusive`, ошибки по зонам, рабочий диапазон, график
и показатели достоверности. Сравнение отклоняется, если изменились громкость,
микрофон, устройства или обработка. Улучшение общей стоимости не считается победой,
если отдельная оптимизируемая зона ухудшилась более чем на допустимый порог.
Quick создаёт только состояние `quick_candidate`; технический кандидат требует
полного измерения и прослушивания.

## Ручная настройка Aura Lab

Изменения в приложении вносит заказчик по точной таблице от программы/оператора.
Автоматическое управление Phone Link не используется. Перед каждым измерением
фиксируются канал, частота, gain и Q; HPF, LPF, Level, Phase, Balance и Fader
не меняются без отдельного этапа настройки.

Текущий лучший комбинированный пресет относится только к общей стереопаре
каналов 1-2:

- 135 Гц: +1 дБ, Q=2,5;
- 600 Гц: +1 дБ;
- 800 Гц: -1 дБ;
- 1 кГц и 1,5 кГц: -3 дБ;
- 2,5 кГц: -4 дБ;
- 3 кГц: -3 дБ;
- 3,5 кГц: -4 дБ;
- 4 кГц: -9 дБ;
- 5 кГц и 6,5 кГц: -3 дБ;
- 8 кГц: -8 дБ;
- 12 кГц: 0 дБ; отдельной полосы 12,5 кГц в Aura Lab нет;
- Q полосы 135 Гц: 2,5; Q остальных перечисленных полос: 6;
- все остальные полосы: 0 дБ.

Balance и Fader остаются по центру. HPF, LPF и поканальный Gain не менялись.
Профиль проверен в центре, возле левого и правого уха и подтверждён контрольным
прослушиванием как лучший из измеренных вариантов. Среднее абсолютное отклонение
от `warm_driver` в диапазоне 100-18 000 Гц составляет 3,48 дБ в центре, 3,33 дБ
возле левого уха и 3,76 дБ возле правого уха.

#### Проверка линейности по громкости

С текущими EQ и Time Alignment выполнены центральные stereo-замеры при громкости
магнитолы 20, 30 и 34 из 62. Ноутбук оставался на 65%, положение микрофона и
остальные настройки не менялись. Громкость 40 исключена до запуска: по измеренному
росту уровня она вывела бы пик записи примерно к -3 дБFS, за рекомендуемое окно
от -12 до -6 дБFS.

- при 20 получен один принятый замер с пиком -24,94 дБFS;
- при 30 приняты два повтора с пиками -13,76 и -13,08 дБFS;
- при 34 два итоговых повтора с пиками -10,16 и -10,39 дБFS совпали по форме в
  пределах 0,74 дБ; более ранний выброс и записи с нераспознанными временными
  маркерами в сравнение не включены;
- от 20 до 30 медианный прирост уровня составил 10,36 дБ, но максимальное
  изменение формы АЧХ достигло 1,48 дБ;
- от 30 до 34 медианный прирост составил 4,12 дБ, однако устойчивое изменение
  формы около 1,25 кГц достигло 6,84 дБ.

Линейность формы АЧХ по громкости не подтверждена, поэтому настройка сохраняется
для опорного уровня 24 из 62. В диапазоне 100-160 Гц повышение громкости не даёт
равномерного подъёма: относительно общего уровня изменения от 20 до 34 составили
примерно +1,19 дБ на 100 Гц, +0,15 дБ на 125 Гц и +1,58 дБ на 160 Гц. Эти данные
не обосновывают большой положительный Gain; допустима только отдельная осторожная
проверка широкого подъёма низов на опорной громкости.

После верхней точки громкость возвращена на 24 и выполнены два контрольных
замера. Общий уровень вернулся к исходной серии с разницей около 0,04 дБ, однако
форма отличалась от прежнего среднего до 3,89 дБ возле 1,25 кГц. Между собой два
возвратных повтора также разошлись до 1,53 дБ при лимите 1 дБ. Следовательно,
изменения длинной серии нельзя однозначно приписывать регулятору громкости:
возможны смещение или вибрация микрофона, изменение положения человека в салоне,
оборотов двигателя, фонового шума либо теплового состояния аудиосистемы. Перед
следующей положительной коррекцией EQ требуется новая стабильная тройная серия
на опорной громкости 24 в строго неизменных условиях.

#### Коррекция нижнего диапазона полосой 135 Гц

В Aura Lab нет полосы 125 Гц; ближайшие доступные регуляторы — 110 и 135 Гц.
Q регулируется от 0 до 12 с шагом 0,5. Для широкого устойчивого дефицита
100-160 Гц при неизменном Gain `+1 дБ` проверены Q=3, Q=2 и Q=2,5, по три
центральных stereo-замера на вариант.

| Настройка 135 Гц | MAE 100-10 кГц, дБ | MAE 100-18 кГц, дБ | Ошибка 100 Гц, дБ | Ошибка 125 Гц, дБ | Ошибка 160 Гц, дБ |
| --- | ---: | ---: | ---: | ---: | ---: |
| +1 дБ, Q=3 | 3,26 | 3,52 | -3,66 | -3,10 | -4,36 |
| +1 дБ, Q=2 | 3,30 | 3,62 | -1,83 | -1,22 | -3,44 |
| +1 дБ, Q=2,5 | 3,23 | 3,60 | -1,85 | -1,19 | -3,55 |

Q=2,5 выбран как лучший компромисс: он сохраняет практически всё улучшение
100-160 Гц от Q=2, но даёт меньшую общую ошибку в основном диапазоне до 10 кГц.
Серия Q=2,5 принята по качеству, клиппинга и предупреждений нет; максимальное
стандартное отклонение выровненной АЧХ составляет 0,35 дБ.

#### Методика выбора лучшего профиля

Цели настройки, обязательные метрики, порядок экспериментов и правила выбора
победителя вынесены в [`TUNING-METHODOLOGY.md`](TUNING-METHODOLOGY.md). Каждый
анализ обязан учитывать MAE, медианное абсолютное отклонение, 75-й процентиль,
максимум, повторяемость, ошибки по зонам и цену требуемой коррекции. Ни одна
метрика и ни одна позиция микрофона не используются как единственное основание.

#### Time Alignment: выбранный профиль 51/0

Канал 1 подтверждён как левый, канал 2 — как правый. Из точки прослушивания
измерены следующие расстояния: до левой пищалки 73 см, до левого дверного
динамика 83,5 см, до правой пищалки 111 см и до правого дверного динамика
123,5 см. Разница между сторонами составляет примерно 38-40 см, поэтому в Aura
Lab последовательно проверены профили компенсации от 30 до 54 см. Итоговый
профиль: `CH1 (левый) = 51 см`, `CH2 (правый) = 0 см`; поканальный Gain оставлен
равным 0 дБ. В Aura Lab используется дискретная шкала, поэтому проверялись только
доступные значения задержки.

Контрольная серия из трёх центральных stereo-замеров принята: максимальное
стандартное отклонение составило 0,91 дБ при лимите 1 дБ. В диапазоне
100-10 000 Гц среднее абсолютное отклонение от `warm_driver` уменьшилось с
5,58 дБ для задержек 0/0 до 4,59 дБ для 40/0. Наиболее заметно улучшились
200-500 Гц, 800 Гц и область 4-8 кГц. Одновременно провал около 630 Гц стал
глубже примерно на 3,5 дБ, а отклонения на 1-1,25 кГц увеличились.

Первая серия 40/0 имела повышенный разброс, но вторая неизменная серия прошла
лимит и подтвердила близкую среднюю форму. Совместная проверка всех шести
измерений даёт максимум 1,29 дБ. После неё выполнены отдельные повторяемые серии
для 30/0, 34/0 и 37/0:

| Задержка CH1/CH2 | MAE 100-10 кГц, дБ | MAE 100-18 кГц, дБ | Макс. std, дБ | Вывод |
| --- | ---: | ---: | ---: | --- |
| 0/0 | 5,58 | 5,48 | 0,68 | исходная точка |
| 30/0 | 4,56 | 4,74 | 0,74 | лучше нижняя середина, хуже верх |
| 34/0 | 5,20 | 5,08 | 0,56 | исключён как проигрывающий соседним вариантам |
| 37/0 | 4,78 | 4,56 | 0,56 | лучший полный диапазон, но сохраняет пик 4 кГц |
| 40/0 | 4,59 | 4,59 | 0,91 | выбранный профиль |

Таблица выше отражает промежуточный этап поиска. Затем дополнительно проверены
44/0, 47/0, 51/0 и 54/0, по три неизменных stereo-замера на вариант. У 47/0 была
небольшая формальная выгода по общей ошибке, но 51/0 дал более подходящую основу
в нижнем и среднем диапазонах и оказался устойчивым при дальнейшей настройке EQ.
После отдельных замеров левого и правого каналов, пространственной проверки и
прослушивания выбран профиль `CH1 = 51 см`, `CH2 = 0 см`, Gain обоих каналов 0 дБ.

### Принцип коррекции пиков и глубоких провалов

Цель настройки — уменьшать устойчивое, слышимое и повторяемое отклонение от
целевой кривой `warm_driver`, сохраняя запас мощности и не создавая новых
дефектов. Нулевая ошибка в каждой отдельной измерительной точке не является
самостоятельной целью.

- устойчивые пики корректируются преимущественно срезом Gain: это сохраняет
  headroom и обычно надёжнее небольших перемещений микрофона;
- узкие или глубокие провалы нельзя автоматически компенсировать равным
  положительным Gain: при фазовом или пространственном погашении усиление почти
  не заполняет провал, но увеличивает мощность, ход динамика и риск клиппинга;
- перед подъёмом провала проверяются левый, правый и совместный каналы, несколько
  близких положений микрофона, полярность подключения и доступные задержки/фаза;
- положительная коррекция допускается только для широкого и пространственно
  устойчивого дефицита, небольшим шагом и после проверки запаса по уровню;
- после каждой итерации выполняются повторные замеры, поскольку Gain одной полосы
  не обязан давать изменение АЧХ один к одному, а соседние фильтры взаимодействуют.

До настройки задержек в текущем EQ сохранялись глубокие провалы около 400-630 Гц,
а прежние пики 4 и 8 кГц были заметно уменьшены последовательными срезами Gain.
Исследование Time Alignment завершено выбором 51/0. Оно уменьшило часть отклонений
нижнего и среднего диапазонов, но провалы 400-630 Гц сохранились и существенно
меняются между положениями микрофона. Поэтому они не компенсируются большим
положительным Gain. Следующим допустимым кандидатом остаётся только небольшой
широкий подъём устойчивого дефицита 100-160 Гц после проверки линейности системы
на нескольких уровнях громкости.

#### Результат центральной L/R/stereo-диагностики

Для текущего EQ выполнены отдельные quick-замеры `left` и `right`, которые
сопоставлены со средним трёх stereo-повторов в неизменном положении микрофона.
Сравнение выполнялось по raw-уровням, поскольку независимое выравнивание каналов
может скрыть реальное сложение или погашение.

- выраженное взаимное погашение обнаружено около 200 Гц (`-5,27 дБ` относительно
  более сильной стороны), 250 Гц (`-2,79 дБ`) и 500 Гц (`-2,66 дБ`);
- на 400 Гц stereo ниже левого канала только на `0,82 дБ`, что сопоставимо с
  измерительным разбросом; правый канал при этом слабее левого примерно на 7 дБ;
- на 630 Гц stereo выше более сильной стороны на `2,95 дБ`: левый и правый
  каналы складываются конструктивно, поэтому глубокий провал 630 Гц не объясняется
  взаимным погашением L/R;
- на 800 Гц и 1 кГц подтверждено нормальное конструктивное сложение каналов.

Подъём 500 Гц не используется как коррекция, поскольку там подтверждено
погашение. Отдельной полосы 630 Гц в Aura Lab нет; доступны 600 и 700 Гц.
Характеризационный тест 600 Гц `+1 дБ`, `Q=6` выполнен и оставлен в текущем
кандидате, однако он не устранил дефицит измерительной полосы 630 Гц. Увеличивать
Gain дальше небезопасно: сначала проверяется, можно ли уменьшить провал изменением
задержки без потери улучшений на остальных частотах.

Схема подключения подтверждена отключением групп в Aura Lab: отключение 1-2
глушит все динамики, а отключение 3-4, 5-6 и SW не меняет звук. Fader в Rear
также глушит систему, а Balance Left/Right корректно разделяет стороны. Канал
1-2 является единственной активной общей стереопарой Front; дверные динамики и
пищалки работают совместно через параллельное или пассивно разделённое подключение.
Группы 3-4, 5-6 и SW не настраиваются.

Профиль формально фиксирует прямой AUX без OEM de-EQ, отсутствие loopback и
пассивную двухполосную топологию. Поэтому программа не разрешает рекомендации по
активному полосному кроссоверу/полярности и не выдаёт фазовые выводы по раздельным
прогонам. Текущие EQ, TA и HPF/LPF этим изменением не меняются.

## Полный режим

```powershell
.\.venv\Scripts\python.exe audio_tuning.py full `
  --device-profile .\devices\aura_indigo_877dsp_mkii.json `
  --profile-name full_candidate `
  --input-device 42 `
  --output-device 21 `
  --session-purpose baseline `
  --channel stereo `
  --split-streams
```

На каждый preset выполняются шесть 8-секундных sweep в строгом порядке:

1. левое ухо;
2. правое ухо;
3. 6 см впереди середины между ушами;
4. 6 см позади середины между ушами;
5. 6 см выше середины между ушами;
6. 6 см ниже середины между ушами.

Перед каждым воспроизведением программа повторно показывает весь порядок, отмечает
текущую позицию и ждёт Enter. Между позициями нельзя менять ориентацию капсюля,
штатив, сиденье, входной gain, громкость или настройки тракта.

Полный отчёт содержит среднюю форму АЧХ, P10/P90, стандартное отклонение, SNR,
confidence, экспериментальные ранние H2/H3, наблюдаемый и оптимизационный диапазоны и подробную
стоимость. В score дополнительно всегда выводятся MAE, медиана, P75, максимум и
знаковые экстремумы. Full-сравнение возвращает continue, stop_recommended или
insufficient_evidence, но остановка требует прослушивания. Следующая настройка DSP
рассчитывается регуляризованным решением только
по измеренной матрице `frequency × control`. Подъём запрещается для узких провалов
и зон с пространственным разбросом выше 3 dB.

Кандидат измеряется с `--session-purpose candidate` и ссылкой на агрегированный
baseline через `--baseline-result`. Положительное full-сравнение переводит результат
только в `listening_confirmation_required`. После прослушивания оператор выполняет:

```powershell
.\.venv\Scripts\python.exe audio_tuning.py full `
  --confirm-listening-result .\data\outputs\...\full_comparison.json `
  --confirmed-by operator `
  --listening-notes "Проверено на знакомых композициях"
```

Только это действие создаёт `confirmed_preset.json`.

## Поканальная диагностика

`--channel left`, `--channel right` и `--channel stereo` являются параметрами обоих
режимов. Для полной диагностики выполняются три независимые full-сессии 6/6. Затем:

```powershell
.\.venv\Scripts\python.exe audio_tuning.py full `
  --compare-channels .\left\spatial_response.json .\right\spatial_response.json .\stereo\spatial_response.json
```

Отчёт сравнивает уровни, форму, рабочий диапазон, confidence, экспериментальные
H2/H3, пространственный разброс и дефекты, появляющиеся только при stereo. Выводы
по каналам разрешены только при `--channel-routing-verified`. Абсолютная задержка
L/R между отдельными Bluetooth-прогонами и полярность не заявляются.

## Перед первым звуком

Оба режима сначала печатают:

- устройство и профиль;
- номер выхода и входа;
- чем воспроизводится и записывается сигнал;
- диапазон, длительность и число воспроизведений;
- зафиксированные уровни громкости;
- ожидаемые выходные файлы.

Затем программа останавливается и ждёт подтверждения Enter. `--dry-run` готовит
сигналы и показывает протокол без звука. `--yes` доступен только быстрому режиму.
В полном режиме подтверждение каждой перестановки микрофона обязательно.

## Контроль качества

Для каждого замера сохраняются peak/RMS/crest factor, клиппинг, dropout, шумовой
фон, SNR, confidence и экспериментальные ранние H2/H3 по 31 третьоктавной полосе.
Dropout анализируется только внутри активного ESS; pre-roll и post-roll в него не
входят. Клиппинг, dropout или неполный ESS делают замер непригодным. Полосы с низким
SNR, сильным roll-off, аномальной ранней H2/H3-оценкой, плохой повторяемостью
или зависимой от громкости формой исключаются из оптимизации, но остаются в
диагностике.

В режиме `--split-streams` запись продолжается ещё две секунды после завершения
воспроизведения, чтобы конечная timing marker не обрезалась задержкой Windows.
Идентификаторы WAV рассчитываются по декодированным аудиоданным, поэтому служебные
timestamp и другие поля контейнера не нарушают сравнение одинаковых ESS-сигналов.

Oklick SM-700G имеет паспортный диапазон `100-18000 Hz`, кардиоидную направленность
и чувствительность `-33±2 dB`. Чувствительность не заменяет индивидуальную SPL-
калибровку. Ниже 100 Hz данные могут оставаться сравнительной диагностикой при
достаточном SNR, но исключаются из целевой оптимизации. Фронт капсюля всегда
направляется к центру лобового стекла/торпедо и не поворачивается между позициями.

Без индивидуального калибровочного CSV результат остаётся сравнительным. Абсолютный
SPL не рассчитывается. Классическая coherence без отдельного опорного входа не
имитируется.

## Проверки

```powershell
 .\.venv\Scripts\python.exe run_tests.py
.\.venv\Scripts\python.exe audio_tuning.py quick --help
.\.venv\Scripts\python.exe audio_tuning.py full --help
```

Автоматические тесты проверяют программные ограничения, но не акустическую точность.
После изменений необходимы новые измерения в автомобиле.

Низкоуровневые `generate_test_signals.py`, `measure_playrec.py`,
`analyze_recordings.py`, `aggregate_spatial.py` и `validate_quick_series.py`
сохранены для внутренних стадий, диагностики и повторного анализа, но не являются
отдельными пользовательскими режимами.
